{
  "name": "sistema-asistencia-docente",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de Control de Asistencia Docente (sitio estático, sin build).",
  "scripts": {
    "deploy:firebase": "firebase deploy --only hosting",
    "deploy:netlify": "netlify deploy --prod --dir=."
  }
}
